# Claude Code

Claude-specific templates live here. Shared Sober source files do not.

- `settings.json`: hook/settings template merged into `~/.claude/settings.json`
- `settings.local.example.json`: optional user-local seed

Shared commands, rules, skills, scripts, and the policy spine live under `.sober/`.
