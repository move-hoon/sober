# Codex Entry Notes

Sober's canonical policy spine is `.sober/AGENTS.md`.

In this repository, root `AGENTS.md` points to that canonical file so Codex can read the same contract without a duplicate copy. During install, Sober writes or links the same spine into `~/.codex/AGENTS.md`.
